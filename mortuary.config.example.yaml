# 🏛️ Code Mortuary 配置
# ⚠️ 警告：删除代码是敏感操作，请仔细阅读下方说明

# ============================================
# ⚠️ 重要警告
# ============================================
# 删除代码是敏感操作！
# - 墓碑一定会生成（这是我们的核心）
# - 删除代码需要手动开启权限
# - 默认只生成墓碑，不删除任何代码
# - 开启前请确保理解后果
# ============================================

cemetery:
  # 是否允许删除代码
  # ⚠️ false = 只生成墓碑（推荐）
  # ⚠️ true = 生成墓碑 + 删除代码（危险！）
  allow_delete: false

  # 删除前是否确认
  # true = 执行删除前询问用户
  # false = 直接删除（仅当 allow_delete: true 时生效）
  confirm_before_delete: true

  # 墓地仓库地址
  # 格式: owner/repo
  cemetery_repo: "Zhifeng-Niu/code-corpses"

  # 扫描阈值（天）
  # 超过这个天数没动的代码 = 死代码
  scan_threshold_days: 90

# ============================================
# 墓碑配置（一定会执行）
# ============================================
tombstone:
  # 是否生成墓碑
  # ⚠️ 这是核心功能，必须开启
  enabled: true
  
  # 墓碑保存位置
  # - "cemetery": 存入墓地仓库
  # - "local": 保存在本地
  # - "both": 两者都保存
  save_to: "cemetery"

  # 墓碑格式
  format: "markdown"

  # 死因自动生成
  auto_death_reason: true

# ============================================
# 删除配置（需要 allow_delete: true）
# ============================================
delete:
  # 是否允许删除
  # ⚠️ 强烈建议保持 false
  enabled: false
  
  # 删除模式
  # - "move": 移送到墓地后再删除
  # - "copy": 只复制到墓地，不删除
  # - "reference": 只生成墓碑引用，不操作原文件
  mode: "move"

  # 备份原文件
  backup: true
  backup_location: "./backup"

# ============================================
# 示例配置
# ============================================

# 安全配置（推荐）：
# cemetery:
#   allow_delete: false
#   confirm_before_delete: true
# tombstone:
#   enabled: true
# delete:
#   enabled: false

# 危险配置（仅当你充分理解后果后使用）：
# cemetery:
#   allow_delete: true
#   confirm_before_delete: false
# delete:
#   enabled: true
#   mode: "move"

# ============================================
# 使用方法
# ============================================

# 1. 复制此文件
# cp mortuary.config.example.yaml mortuary.config.yaml

# 2. 编辑配置
# nano mortuary.config.yaml

# 3. 运行
# mortuary embalm my-repo "src/old" "不再维护"

# ⚠️ 删除代码前请确保：
# 1. 备份重要代码
# 2. 测试环境先试运行
# 3. 理解撤销非常困难
# ============================================

# 🤖 Code Corpses Agent Configuration
# 让你的 Coding Agent 自动管理代码墓地

# ============================================
# 墓地管理员配置
# ============================================

cemetery:
  # 是否启用墓地管理
  enabled: true
  
  # 扫描频率
  # - hourly: 每小时
  # - daily: 每天
  # - weekly: 每周（推荐）
  # - monthly: 每月
  scan_interval: weekly
  
  # 死代码判定阈值（天）
  # 超过这个天数没动的代码 = 死代码
  dead_threshold_days: 90
  
  # 是否自动归档
  # true: 自动把死代码归档到墓地
  # false: 只标记，等你确认
  auto_archive: false
  
  # 诈尸检测
  zombie_detection:
    enabled: true
    # 新项目创建时自动检测
    auto_check_on_new_project: true
    # 检测到诈尸时的通知方式
    notify: telegram

# ============================================
# 通知配置
# ============================================

notifications:
  # 通知渠道
  channel: telegram
  
  # 通知事件
  events:
    new_corpse: true       # 发现新尸体
    zombie_alert: true     # 诈尸通知
    weekly_report: true    # 每周统计
    monthly_report: true   # 每月报告

# ============================================
# GitHub 集成
# ============================================

github:
  # 扫描的仓库列表
  # - ["*"] = 扫描所有仓库
  # - ["my-project", "old-app"] = 只扫描指定仓库
  repos:
    - "*"
  
  # 排除的仓库（不扫描）
  exclude:
    - "private-repo"
    - "archived-repo"
  
  # 排除的文件模式
  exclude_patterns:
    - "*.test.ts"
    - "*.spec.ts"
    - "node_modules/**"
    - "dist/**"
    - ".git/**"

# ============================================
# 墓地统计
# ============================================

stats:
  # 是否记录统计数据
  enabled: true
  
  # 统计项目
  track:
    corpses_count: true        # 尸体数量
    zombie_count: true         # 诈尸数量
    longest_sleeping: true      # 躺尸最久的代码
    most_killer: true          # "杀人" 最多的仓库

# ============================================
# Agent 行为规则
# ============================================

agent_rules:
  # 当 Agent 发现可复用的死代码时
  when_reuse_dead_code:
    # 询问用户是否复用
    ask_first: true
    
    # 自动复用（需要 ask_first: false）
    auto_reuse: false
    
    # 复用时添加注释
    add_resurrection_comment: true
    resurrection_comment: |
      # 🧟 此代码诈尸了！
      # 来自墓地: {corpse_repo}/{corpse_path}
      # 原死亡日期: {death_date}
      # 复活日期: {today}

# ============================================
# Vibe 配置（快乐最重要 🎉）
# ============================================

vibe:
  # 墓志铭生成风格
  epitaph_style: funny  # funny | poetic | neutral
  
  # 是否使用彩蛋
  enable_easter_eggs: true
  
  # 快乐模式
  # true: 每次扫描后发个搞笑的通知
  # false: 只发统计
  happy_mode: true

# ============================================
# 示例通知消息
# ============================================

example_messages:
  new_corpse: |
    🪦 发现新尸体！
    {repo}/{path} 已经 {days} 天没人疼了
    
  zombie_alert: |
    🧟 诈尸啦！！！
    {corpse} 在 {new_repo} 复活了
    相似度: {similarity}%
    
  weekly_report: |
    📊 本周墓地统计
    - 新增尸体: {new_corpses}
    - 诈尸次数: {zombies}
    - 最久躺尸: {oldest}

# ============================================
# 使用方法
# ============================================

# 1. 复制这个文件到你的项目根目录
# 2. 修改上面的配置
# 3. 让 Agent 读取这个配置来管理墓地
#
# Agent 会自动：
# - 定期扫描代码
# - 标记死代码
# - 生成墓碑
# - 检测诈尸
# - 发通知给你

# 💡 提示：快乐是开源的，墓地是热闹的！
